const app=require("./app");
let assert=require("chai").assert;

describe("integrating TravisCI with github", test);
function test(){
        it("should return a number", ()=>{
            assert.isNumber(app,"Result should be a number")
        });
}
